import { SpeciesFilterPipe } from './species-filter.pipe';

describe('SpeciesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SpeciesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
